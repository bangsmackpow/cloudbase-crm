{
  "name": "chalkboard-crm",
  "version": "1.0.0",
  "description": "Premium multi-tenant CRM on Cloudflare Edge",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply chalkboard-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply chalkboard-db --remote"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
